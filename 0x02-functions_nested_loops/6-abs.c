#include "main.h"
#include <math.h>
/**
 * _abs - computes absolute value of an integer
 * Return: absolute integer value
 * @n: takes in an integer
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
