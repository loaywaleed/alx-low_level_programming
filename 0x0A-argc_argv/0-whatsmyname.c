#include <stdio.h>
#include <stdlib.h>
/**
 * main - prints the name of the program
 * @argc: number of commands
 * @argv: array name
 * Return: 0
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
