#include "main.h"

/**
 * _puts - prints a string, followed by a new line
 * Return: void
 * @str: input string
 */

void _puts(char *str)
{
	while (str[i])
	{
		_putchar(str[i]);
	}
	_putchar('\n');
}
